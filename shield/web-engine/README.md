## Nginx Ingress Controller Deployment in GKE Cluster

# Prerequesties
```
   - kubectl
   - GKE Cluster
```

# Nginx Deploy

  - Run the below command to deploy the nginx controller.
  - Ref (https://kubernetes.github.io/ingress-nginx/deploy/)

```bash
  kubectl apply -f .
```
